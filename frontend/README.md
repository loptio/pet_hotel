# frontend/ — React（在 Claude Code 實作）

對著 `../contracts/`（API 契約）做。視覺與 design system 來自 **Claude Design** 的「Handoff to Claude Code」產物（Claude Design 只做設計/原型、不接後端、不直接產 React）。
按角色 4 套界面（RBAC）：飼主 / 櫃台 / 美容師 / 管理員；先做飼主預約主流程。
（給 Claude Design 連結時，連這個 `frontend/` 子目錄即可。）
